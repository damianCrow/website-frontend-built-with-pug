'use strict';

import FanBoostOpen from '../fan-boost-open';

describe('FanBoostOpen View', function() {

  beforeEach(() => {
    this.fanBoostOpen = new FanBoostOpen();
  });

  it('Should run a few assertions', () => {
    expect(this.fanBoostOpen);
  });

});
