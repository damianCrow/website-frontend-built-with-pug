'use strict';

import CloseButton from '../close-button';

describe('CloseButton View', function() {

  beforeEach(() => {
    this.closeButton = new CloseButton();
  });

  it('Should run a few assertions', () => {
    expect(this.closeButton);
  });

});
