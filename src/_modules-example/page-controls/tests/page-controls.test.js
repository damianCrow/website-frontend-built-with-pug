'use strict';

import PageControls from '../page-controls';

describe('PageControls View', function() {

  beforeEach(() => {
    this.pageControls = new PageControls();
  });

  it('Should run a few assertions', () => {
    expect(this.pageControls);
  });

});
