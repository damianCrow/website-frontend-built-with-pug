'use strict';

import ThirdNav from '../third-nav';

describe('ThirdNav View', function() {

  beforeEach(() => {
    this.thirdNav = new ThirdNav();
  });

  it('Should run a few assertions', () => {
    expect(this.thirdNav);
  });

});
