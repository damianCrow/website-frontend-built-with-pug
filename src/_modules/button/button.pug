mixin button(link, href)
  - href = href || '#'
  
  if link
    a.button(class!=attributes.class, type!=attributes.type href!=href)
      span
        block
  else
    button.button(class!=attributes.class, type!=attributes.type)
      span
        block
